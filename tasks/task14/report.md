The bandit reports can be found in the json fles in this directory

The scans were only on the src folder, since it's the only folder with actually my code

For the security tests, the assert remains, but is commented with no-sec, since those asserts are _only_ for tests
