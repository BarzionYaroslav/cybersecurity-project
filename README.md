# Project for Cybersecurity

## Placement of tasks

Tasks can be found in the folder /tasks

## Launch

If the code is to be launched without docker-compose, use `uvicorn main:app --app-dir src` instead of old way of using `cd src` and then `uvicorn main:app`
